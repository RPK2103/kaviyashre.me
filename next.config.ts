import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // Allow the profile image to be served from /public/images
    // Add external domains here when needed (e.g. for CDN-hosted assets)
  },
};

export default nextConfig;
